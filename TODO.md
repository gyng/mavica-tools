# TODO

- general UI/UX improvements: breadcrumbs, wizard flows
- split format/create image screen
- do a extract raw .img file
- add dropdown w/ autocomplete for model
- keyboard shortcuts for screens, support mouse back/forward
- test video with fd88
- Greaseweazle/Kyroflux support (I don't have the hardware!)

- tagging step
- per batch/disk
- check if already tagged
- show table form in viewer
- ???
- import photos is failing but the diskchecker/scanner works. have a fallback for bad floppies -- import sohuld be resilient to bad floppies as well as far as possible
- next disk needs to be clickable
- floppy autodetection in import from floppy only works once??
- during import the defrag doesn't update with read sectors?? different reading method?
- columns needed for image status
- add open file link in file browsers